name: text_component
description: "A customizable Flutter widget for displaying text with advanced styling options such as font size, color, decoration, and more."
version: 1.0.0
homepage: https://github.com/raihansikdar/text_component.git

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

